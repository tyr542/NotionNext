# GEMINI.md

這是 NotionNext 個人部落格 fork，工作區位於 `D:\workspaces\notionnext`。
請先讀同目錄 `CLAUDE.md`，再依 hand-off 指向的熱區檔案工作。
常用驗證：`npm run lint`、`npm run type-check`、`npm test`、`npm run build`。
發布或交付前，遵循 `D:\coding\.agent\agents\mirror-of-truth\AGENT.md` 的守門協議。
